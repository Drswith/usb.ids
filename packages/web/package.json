{
  "name": "@usb-ids/web",
  "version": "0.0.0",
  "private": true,
  "description": "Private Vite web UI package for usb.ids.",
  "type": "module",
  "scripts": {
    "lint": "oxlint app",
    "dev": "vite --port 5173",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@usb-ids/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^6.2.1"
  }
}
