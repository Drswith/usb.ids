## 1. Package README

- [x] 1.1 Add a CLI-first README under `packages/cli`.
- [x] 1.2 Include `README.md` in the CLI package file manifest.
- [x] 1.3 Restore the root `lint-staged` script entrypoint for existing local hooks.

## 2. Validation

- [x] 2.1 Run OpenSpec validation.
- [x] 2.2 Run a package dry run and confirm `README.md` is present in the tarball contents.
