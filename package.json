{
  "name": "usb-ids-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace orchestration for usb.ids monorepo.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "pnpm -r --filter \"./packages/*\" --if-present run lint",
    "typecheck": "pnpm -r --filter \"./packages/*\" --if-present run typecheck",
    "test": "pnpm -r --filter \"./packages/*\" --if-present run test",
    "build": "pnpm -r --filter \"./packages/*\" --if-present run build",
    "fetch-usb-ids": "pnpm --filter \"./packages/cli\" --if-present run fetch-usb-ids",
    "diff-hash": "pnpm --filter \"./packages/cli\" --if-present run diff-hash",
    "build:artifacts": "pnpm --filter \"./packages/cli\" --if-present run build:artifacts",
    "openspec": "pnpm exec openspec",
    "openspec:list": "pnpm exec openspec list",
    "openspec:status": "pnpm exec openspec status",
    "openspec:instructions": "pnpm exec openspec instructions",
    "openspec:validate": "pnpm exec openspec validate --all"
  },
  "devDependencies": {
    "@fission-ai/openspec": "^1.3.1",
    "@types/node": "^22.13.10",
    "@vitest/coverage-v8": "^3.2.4",
    "happy-dom": "^18.0.1",
    "oxfmt": "^0.49.0",
    "oxlint": "^1.64.0",
    "tsdown": "^0.14.2",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vite": "^6.2.1",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.15.9"
}
